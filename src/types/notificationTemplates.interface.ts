export type NotificationTemplatesType = {
    _id?: string,
    title: string,
    message: string,
    requiredKeys: string[],
    name: string,
    desc: string,
    CreatedAt?: Date,
    UpdatedAt?: Date
}